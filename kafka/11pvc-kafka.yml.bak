apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: kafka-broker-pvc
  namespace: dev
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: kafka-broker
  resources:
    requests:
      storage: 30Gi
